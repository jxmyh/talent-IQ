{
  "name": "talent-iq",
  "version": "1.0.0",
  "type": "module",
  "main": "./backend/src/server.js",
  "scripts": {
    "init": "npm install --prefix backend && npm  install --prefix frontend && npm run build --prefix frontend",
    "build": "npm run start --prefix frontend",
    "start": "npm run start --prefix backend",
    "vercel-build": "npm install --prefix backend && npm  install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}